# Carregado automaticamente pelo Expo quando `expo start` roda em modo development.
# Apenas variáveis com prefixo EXPO_PUBLIC_ ficam disponíveis no app (ver core/config/env.ts).
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
