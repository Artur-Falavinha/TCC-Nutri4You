// Ambiente de DESENVOLVIMENTO.
// Usado automaticamente por `ng serve` / `ng build --configuration development`.
export const environment = {
  production: false,
  // API rodando localmente (Spring Boot, Sprint 1: endpoint /api/v1/health).
  apiBaseUrl: 'http://localhost:8080/api/v1'
};
